feat: per-section prompt templates with individual regenerate buttons

- Add section field (system_prompt/first_message/qualification_prompt) to PromptTemplate
- Replace single generate_prompts() with generate_section() — plain text per section, no JSON
- Add GenerateSectionView: 3 sequential AJAX calls, auto-saves each field on existing positions
- Position form: Generate All button + individual Regenerate button per prompt field
- ToggleActiveView: deactivate per section only (not globally)
- Templates list: per-section health dashboard (green/yellow status cards)
- Template form: section selector, updated help text, single-value test generate output
- Update docs: specs.md and README.md fully reflect all changes from this and prior session
